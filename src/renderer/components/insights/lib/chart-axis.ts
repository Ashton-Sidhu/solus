/** Clear space between a time axis baseline and its labels. */
export const TIME_AXIS_LABEL_GAP_PX = 8;

/** Room for the time label plus its gap, without clipping the chart. */
export const TIME_AXIS_INSET_PX = 24;
