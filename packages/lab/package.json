{
  "name": "@solus/lab",
  "private": true,
  "type": "module",
  "description": "The Lab: a programmatic multi-persona client, an isolated host, and scripted scenarios for multiplayer (docs/plans/multiplayer-sharing.md §8). Never bundled into a release.",
  "exports": { "./*": "./src/*.ts" },
  "scripts": { "check": "tsc --noEmit -p tsconfig.json" },
  "dependencies": {
    "@solus/contracts": "workspace:*",
    "socket.io-client": "^4.8.1",
    "zod": "^4.5.4"
  },
  "devDependencies": { "typescript": "^5.9.3" }
}
